// Placeholder — Task 21 replaces with the full Analyze panel
// (gate form + module picker + iframe + history).

export function AnalyzePanel() {
  return (
    <div className="p-6">
      <h2 className="text-lg font-medium mb-2">Analyze (SOP)</h2>
      <p className="text-sm text-muted-foreground">
        Full panel ships in Phase 4 Task 21. The backend is ready —
        POST /research/analyze on port 8001 returns a full SOP report.
      </p>
    </div>
  );
}
